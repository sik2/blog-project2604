spring:
  security:
    oauth2:
      client:
        registration:
          kakao:
            clientId: NEED_TO_NEED
custom:
  jwt:
    secretKey: NEED_TO_NEED
  accessToken:
    expirationSeconds: NEED_TO_NEED